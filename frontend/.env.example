# AYNAM public website (Vercel, root directory = repo root).
# The only runtime config the website needs: where the contact form POSTs to.
NEXT_PUBLIC_API_URL=http://localhost:4000
# production e.g. NEXT_PUBLIC_API_URL=https://api.aynam.in
